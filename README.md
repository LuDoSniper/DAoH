## Différence entre `Master` et `Node`

`Master` : Serveur maitre servant comme "discovery" par les clients. C'est lui qui liste les `Node`

`Node` : Serveur indépendant, ayant sa propre base de donnée donc ses propres users, et evidemment le port pointant vers le bon serveur dédié
